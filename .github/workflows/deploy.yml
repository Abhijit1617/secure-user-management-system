name: Deploy to AWS EC2

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Deploy through SSM
        run: |
          COMMAND_ID=$(aws ssm send-command \
            --instance-ids i-02a2930d909b760bd \
            --document-name "AWS-RunShellScript" \
            --parameters 'commands=[
              "cd /home/ssm-user/secure-user-management-system",
              "sudo -u ssm-user git -C /home/ssm-user/secure-user-management-system pull origin main",
              "sudo docker compose -f /home/ssm-user/secure-user-management-system/docker-compose.yml build backend",
              "sudo docker compose -f /home/ssm-user/secure-user-management-system/docker-compose.yml up -d --force-recreate backend"
            ]' \
            --comment "GitHub Actions deployment" \
            --query "Command.CommandId" \
            --output text)

          echo "SSM Command ID: $COMMAND_ID"

          aws ssm wait command-executed \
            --command-id "$COMMAND_ID" \
            --instance-id i-02a2930d909b760bd

          aws ssm get-command-invocation \
            --command-id "$COMMAND_ID" \
            --instance-id i-02a2930d909b760bd \
            --query '{Status:Status,Output:StandardOutputContent,Error:StandardErrorContent}' \
            --output json